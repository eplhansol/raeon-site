# Cloudflare Pages 배포 설정
#
# 이 저장소는 이미 완성된 정적 사이트다. 빌드할 것이 없고 파일이 루트에 있다.
# 자동 감지가 실패해 "Could not detect a directory containing static files"
# 오류가 나므로 어디를 배포할지 여기서 못박는다.

name = "raeon-site"
compatibility_date = "2026-08-10"
pages_build_output_dir = "."
